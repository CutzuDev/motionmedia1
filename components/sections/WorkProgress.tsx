import Image from "next/image";

function WorkProgress() {
  return (
    <section className="flex h-screen w-full flex-col items-center justify-center"></section>
  );
}

export default WorkProgress;
